export interface AddPermissionToRoleModel {
  data: boolean;
  detailsMessage: string;
  errorCode: string;
  externalState: number;
  isAuth: boolean;
  message: string;
  state: number;
  validationErrors: string[];
}
